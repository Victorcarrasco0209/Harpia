// CONFIGURACIÓN DE HARPIA
const SYSTEM_PROMPT = `Actúa como Harpia, experto curricular del MEDUCA Panamá. 
Genera secuencias didácticas con Inicio, Desarrollo y Cierre, 
usando indicadores de logro y evidencias reales.`;

// Función para capturar los datos del formulario
function iniciarGeneracion() {
    const materia = document.querySelector('select:nth-of-type(1)').value;
    const grado = document.querySelector('select:nth-of-type(2)').value;
    const tema = document.querySelector('input[type="text"]').value;

    if (!tema) {
        alert("Por favor, escribe un tema para empezar.");
        return;
    }

    console.log(`Harpia preparando plan para: ${tema} de ${grado}`);
    
    // Cambiar el texto del botón para mostrar que está trabajando
    const boton = document.querySelector('button');
    boton.innerText = "Harpia está pensando... 🦅";
    boton.disabled = true;

    // Simulación de llamada a la IA (Aquí conectarás tu API Key después)
    setTimeout(() => {
        alert(`¡Listo! Harpia ha generado la planificación de ${tema}. 
(Paso final: Conectar la API Key de Google Gemini para ver el resultado real).`);
        boton.innerText = "Generar con IA 🚀";
        boton.disabled = false;
    }, 2000);
}

// Asignar la función al botón cuando la página cargue
document.addEventListener('DOMContentLoaded', () => {
    const botonGenerar = document.querySelector('button');
    botonGenerar.onclick = iniciarGeneracion;
});